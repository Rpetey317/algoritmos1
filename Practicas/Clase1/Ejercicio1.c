#include<stdio.h>

int main(){

    char letra = '8';

    if (letra >= 'A' && letra <= 'Z'){
        printf("Las mayúsculas son grandes, como bokitaaaa\n");
    } else if (letra >= 'a' && letra <= 'z'){
        printf("Las minúsculas son chikis como river jaja\n");
    } else{
        printf("Pará pará que me estás mandando?!?!?\n");
    }

    return 0;
}
